package test;

public class TestClass {
	
	public String toUpperCase(String one)
	{
		String temp = one.toUpperCase();
		
		return temp;
	}
	
	public int compareStrings(String one,String two)
	{
		return one.compareTo(two);
	}

}
